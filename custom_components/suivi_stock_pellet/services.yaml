log_consumption:
  name: Enregistrer une consommation
  description: Ajoute une entrÃ©e de consommation de granulÃ©s au journal de la saison en cours.
  fields:
    qty_bags:
      name: Nombre de sacs
      description: Nombre de sacs consommÃ©s.
      required: true
      default: 1
      selector:
        number:
          min: 0.1
          max: 100
          step: 0.1
          mode: box
    date:
      name: Date
      description: Date de la consommation (par dÃ©faut aujourd'hui).
      required: false
      selector:
        date: {}

log_purchase:
  name: Enregistrer un achat
  description: Ajoute une entrÃ©e d'achat de granulÃ©s au journal de la saison en cours.
  fields:
    qty_bags:
      name: Nombre de sacs
      description: Nombre de sacs achetÃ©s.
      required: true
      selector:
        number:
          min: 0.1
          max: 1000
          step: 0.1
          mode: box
    price_eur:
      name: Prix total
      description: Prix total de l'achat en euros (facultatif).
      required: false
      selector:
        number:
          min: 0
          max: 10000
          step: 0.01
          mode: box
    date:
      name: Date
      description: Date de l'achat (par dÃ©faut aujourd'hui).
      required: false
      selector:
        date: {}

undo_last_entry:
  name: Annuler la derniÃ¨re saisie
  description: Annule la derniÃ¨re entrÃ©e (achat ou consommation) de la saison en cours (ou d'une saison prÃ©cisÃ©e).
  fields:
    season:
      name: Saison
      description: Saison Ã  corriger, au format AAAA-AAAA (ex. 2024-2025). Par dÃ©faut, la saison en cours.
      required: false
      example: "2024-2025"
      selector:
        text: {}

edit_entry:
  name: Modifier une saisie
  description: Modifie une entree existante (achat ou consommation) du journal d'une saison.
  fields:
    season:
      name: Saison
      description: Saison contenant la saisie a modifier, au format AAAA-AAAA (ex. 2024-2025).
      required: true
      example: "2024-2025"
      selector:
        text: {}
    index:
      name: Index
      description: Position de la saisie dans le journal de la saison (0 = la plus ancienne).
      required: true
      selector:
        number:
          min: 0
          max: 10000
          mode: box
    qty_bags:
      name: Nombre de sacs
      description: Nouvelle quantite de sacs (facultatif, inchangee si absente).
      required: false
      selector:
        number:
          min: 0.1
          max: 1000
          step: 0.1
          mode: box
    price_eur:
      name: Prix total
      description: Nouveau prix total en euros, uniquement pour un achat (facultatif, inchange si absent).
      required: false
      selector:
        number:
          min: 0
          max: 10000
          step: 0.01
          mode: box
    date:
      name: Date
      description: Nouvelle date de la saisie (facultatif, inchangee si absente).
      required: false
      selector:
        date: {}

delete_entry:
  name: Supprimer une saisie
  description: Supprime definitivement une entree (achat ou consommation) du journal d'une saison.
  fields:
    season:
      name: Saison
      description: Saison contenant la saisie a supprimer, au format AAAA-AAAA (ex. 2024-2025).
      required: true
      example: "2024-2025"
      selector:
        text: {}
    index:
      name: Index
      description: Position de la saisie dans le journal de la saison (0 = la plus ancienne).
      required: true
      selector:
        number:
          min: 0
          max: 10000
          mode: box
